# ========================================
# PHP-Nginx-TCP — конфигурация окружения
# ========================================

# Конфигурация PostgreSQL (база данных)
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=app-db
# Хост PostgreSQL для подключений PHP-приложения
PG_HOST=postgres-nginx-tcp

# Конфигурация pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
# Примечание: pgAdmin не поддерживает автонастройку как phpMyAdmin
# Необходимо вручную добавить сервер в UI pgAdmin, используя значение PG_HOST

# Xdebug (опционально; включается через docker-compose.xdebug.yml по умолчанию при использовании `make xdebug-up`)
# Заполняйте эти значения, только если хотите включить Xdebug без overlay compose-файла
XDEBUG_MODE=
XDEBUG_START=
XDEBUG_CLIENT_HOST=host.docker.internal

# ========================================
# PHP-Nginx-TCP — конфигурация окружения
# ========================================

# PostgreSQL Database Configuration
POSTGRES_DB=php-fpm-tcp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
# PostgreSQL host for PHP application connections
PG_HOST=postgres-nginx-tcp

# pgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
# Note: pgAdmin doesn't support auto-configuration like phpMyAdmin
# You need to manually add server in pgAdmin UI using PG_HOST value

# Xdebug (optional; enabled via docker-compose.xdebug.yml by default when using `make xdebug-up`)
# Set these only if you want to enable Xdebug without the overlay compose file
XDEBUG_MODE=
XDEBUG_START=
XDEBUG_CLIENT_HOST=host.docker.internal